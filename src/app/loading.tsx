import React from 'react'

type Props = {}

const Loading = (props: Props) => {
    return (
        <div className="center textAlignCenter" style={{ height: "100vh" }}>
            Loading...
        </div>
    )
}

export default Loading